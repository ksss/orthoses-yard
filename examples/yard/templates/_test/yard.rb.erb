require 'yard'

YARD::Registry.root.children.each do |child|
  child.name(false)
  child.docstring.all
end

types_explainers = ::YARD::Tags::TypesExplainer::Parser.parse("String, Array<String>")
types_explainers.each do |types_explainer|
  types_explainer.types
end
